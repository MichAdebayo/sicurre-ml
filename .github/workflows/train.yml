# name: Trigger Kaggle Retraining

# on:
#   repository_dispatch:
#     types: [trigger-retraining] 

# jobs:
#   run-kaggle-training:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.10'

#       - name: Install Kaggle API
#         run: pip install kaggle

#       - name: Trigger Kaggle Kernel
#         env:
#           KAGGLE_USERNAME: ${{ secrets.KAGGLE_USERNAME }}
#           KAGGLE_KEY: ${{ secrets.KAGGLE_API_KEY }}
#         run: |
#           # This command tells Kaggle to execute your notebook in the cloud
#           kaggle kernels push -p ./path_to_your_kaggle_metadata_folder